RIGHT CLICK - tools menu
W - Color Menu
E - distance menu

K - save
L - load

A - (select two nodes) select connection
S - (select two nodes) deselect connection

D - highlight minimal path

1 or 2 - (with select tool) choose selection id

G - (with selected nodes of one type) connect each node to each other node
G - (with selected nodes of two types) connect each node to each other node in the other selection type
H - (with selected nodes of one type) disconnect each node to each other node
H - (with selected nodes of two types) disconnect each node to each other node in the other selection type